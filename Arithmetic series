def SimpleAdding(num): 
    sum = 0
    for i in range(num):
        sum = sum + num
        num = num - 1
    return sum
print SimpleAdding(raw_input())


def SimpleAdding(num): 
    return sum(range(1, num + 1))
print SimpleAdding(raw_input())
